# Release validation record

Phase-I lightweight suite: **221 passed, 1 deselected**. The deselected test is the explicitly marked full-workspace historical integrity test, requiring omitted intermediates. The controlled release update on 26 September 2026 ran `python -m pip install -r requirements.txt`, the CI import check and `python -m pytest -q` under Python 3.13 in the existing scientific test environment. No scientific model rerun occurred.

`python scripts/verify_release.py`: payload hashes, provenance map, primary cohort and state conclusion PASS. Frozen fold definitions and recovered primary point metrics are checked by tests. `python scripts/reproduce_tables.py`: nine main comparison rows rebuilt. `python scripts/render_journal_figures.py`: six figures rebuilt from frozen summaries. Package wheel built and installed into an isolated local target without dependency installation; this is not a fresh dependency-environment test.

`bash manuscript/build.sh`: main 13 pages and supplement 5 pages compiled on 26 September 2026. Both tracked PDFs were regenerated from their tracked sources. The main source retains the three prior scientific-audit wording corrections and applies only the newly requested author/header change; the supplement source is the verbatim Desktop revision. The supplied Desktop main PDF and an isolated build of its Desktop source have identical text on all 13 pages after whitespace, ligature and line-hyphenation normalization, but typesetting differs. The tracked main PDF instead reflects the retained audit corrections, as clarified by the author. All 18 final pages were rendered and visually inspected. There are no undefined citations or references; the main log retains a balance warning and a 1.232-point overfull vertical box, with no visible clipping. The supplement has no overfull boxes.

Historical original-tree safety audit (not rerun during this update): no original file was added, removed or destructively moved; all 10,148 protected original hashes pass. Six copied provenance files had sub-microsecond mtime rounding changes during coordinated iCloud reads (18–107 ns), with unchanged sizes, modes and copied-source hashes. Those filesystem timestamp adjustments were not restored by writing to the originals. No scientific original bytes were edited.

Phase-II branch validation on 26 September 2026: **221 passed, 1 deselected** with `python -m pytest -q`, plus **59 passed** with `python -m pytest cross_mission/tests -q`. This adds 54 retained cross-mission contracts and five product-path/output-preservation checks. Every tracked `cross_mission/` file remains byte-identical to the pre-merge Phase-II branch, including all pilot records, the original design protocol and lock. X0/X1 remain PASS, X2 NOT PASSED, X3-X7 NOT RUN, and classification NOT YET TESTED. No NuSTARDAS task or measurement fit was rerun during this update.
