# Phase-I preservation

The frozen publication snapshot is the annotated tag `v1.0.0-phase1-paper`, targeting `64afc0407e4d1ffa512c3466c1a8abd2fff7c6f4`. Documentation cleanup advances branch heads without moving that tag.

At the original release preparation, 295 Phase-I source files were selected: 287 byte-identical copies and eight documented operational/dependency/rendering/test-scope adaptations. All selected scientific CSV/JSON records, frozen protocols, manuscript sources, bibliography and figures retained their original bytes. The larger research workspace's 10,148 protected hashes passed that preparation audit. Those historical checks are distinct from the current branch-to-branch curation comparison.

Canonical `manuscript/main.tex` SHA-256: `d8e1782eed39dd05ebc2c6d66c806536531e514cb8ac811cf9f18231233bb269`. The unapproved future-work review copy remains excluded. The supplement keeps small duplicated tables so it remains self-contained.

The release verifier checks the 687-observation, 29-system primary cohort (7 BH/22 NS), its 122/565 observation split and zero state-matched model runs. Tests recover primary point metrics from unchanged predictions and check the recorded grouped/LOSO memberships. Rendering reads stored values without fitting or resampling.

[`../release_provenance.csv`](../release_provenance.csv) records original and packaged hashes. [`../derived_artifacts.json`](../derived_artifacts.json) traces the compact fold definitions to 117 original run records. Historical hashes retain their original meanings. Current curation results are recorded once in [final_pre_push_report.md](final_pre_push_report.md).
